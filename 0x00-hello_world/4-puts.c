#include <stdio.h>

/**
 * main - A simple function that prints a random message
 * Return: on success the main function returns 0, non zero otherwise
 */

int	main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
